# Exam Tutor 环境变量模板
# 复制为 .env 并填入实际值（.env 不会被 git 跟踪）

# DeepSeek API Key（必需）
DEEPSEEK_API_KEY=***

# JWT 签名密钥（必需，用于保持用户登录状态跨服务重启）
# 生成方式：python3 -c "import secrets; print(secrets.token_urlsafe(32))"
EXAM_TUTOR_JWT_SECRET=
